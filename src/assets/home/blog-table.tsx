import React from 'react';

const BlogTable: React.FC = () => {
  // BlogTable content (list of user posts)
  return (
    <div className="blog-table">
      {/* BlogTable content */}
    </div>
  );
};

export default BlogTable;
